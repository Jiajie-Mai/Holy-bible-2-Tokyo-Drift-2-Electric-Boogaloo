document.addEventListener("DOMContentLoaded", () => {
    edosh = document.getElementByID("edosh");
    round = document.getElementByID("round");
    stock = document.getElementByID("stock");
    pdosh = document.getElementByID("pdosh");

});
